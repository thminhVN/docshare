# Copy to ./deploy.env (gitignored) and fill in. Used by ./deploy.sh.
SSH_HOST=docshare@203.0.113.10
BASE=/opt/docshare
SERVICE=docshare
SSH_PORT=22
# Set to "systemctl" if your deploy user is root / doesn't need sudo.
RESTART=sudo systemctl
# Only used for the final "App: https://..." message.
PHX_HOST=docshare.gatetroy.com
